# Local AI runtime state
.atl/

# Build artifacts (root directory only)
/royo-learn
/royo-learn.exe

# GoReleaser output
/dist/

# Temp files
*test_isnotexist*
*Temp/opencode/*

# Personal scratch prompts (one-off LLM executor inputs that landed at
# the repo root during a session and are not project artifacts).
/PROMPT-*.md
